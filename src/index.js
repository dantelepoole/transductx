/**
 * @module index
 * @ignore
 */

'use strict';

module.exports = {
    filter    : require('./filter'),
    reduce    : require('./reduce'),
    transduce : require('./transduce'),
    transform : require('./transform')
}